using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CharacterList : MonoBehaviour
{
	/// I should try to come up with a way to track all the characters without a list like this

	// The list that tracks all the characters
	public List<Character> characterList = new();
}
